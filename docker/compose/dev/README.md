# Dev Compose for thatdam-run

Launches the edge image from the current channel.

## Usage

```bash
cd docker/compose/dev
docker compose --env-file .env.dev up
```

## Testing

Ensure the container starts and logs the stub binary output.
