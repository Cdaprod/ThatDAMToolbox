# Verdaccio Registry Service

Local npm registry for `@cdaprod/*` packages.

## Usage

```bash
# start registry (from repo root)
docker compose -f docker/compose/docker-compose.registry.yaml up -d
```
