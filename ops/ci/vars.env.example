# Registry & namespace (override in CI or local .env)
REGISTRY=ghcr.io
REGISTRY_NS=cdaprod

# Services (logical names)
SERVICE_EDGE=thatdam-run      # single executable / edge agent
SERVICE_API=thatdam-api       # (optional) your cloud API
SERVICE_WEB=thatdam-web       # (optional) your cloud web

# Versioning
# VERSION is immutable SemVer (e.g., 1.4.0). In dev/staging, CI can synthesize.
VERSION=0.0.0-dev
# CHANNEL is lane: dev|staging|prod
CHANNEL=dev

# Build matrix
TARGET_PLATFORMS=linux/amd64,linux/arm64

# Optional build flavor suffix (e.g., -gpu). Leave empty if not used.
IMAGE_SUFFIX=

# OCI labels (injected at build; CI will update these)
GIT_SHA=local
BUILD_DATE=1970-01-01T00:00:00Z
