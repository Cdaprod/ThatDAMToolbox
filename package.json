{
  "name": "thatdamtoolbox",
  "private": true,
  "workspaces": [
    "docker/web-app",
    "docker/web-site",
    "docker/packages/*"
  ],
  "scripts": {
    "build": "echo 'use per-app builds'",
    "lint": "eslint .",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "eslint": "^9.0.0"
  }
}
